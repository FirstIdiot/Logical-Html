class IfStatementElement extends HTMLElement {
    constructor(){
        super();
        const isThere = this.getAttribute('condition');
        //Needs Parsing and alot of headache I don't have an Idea about how to do it 
        console.log('Parsing Missing, Try to implement it')
    }
}